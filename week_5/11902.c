#include <stdio.h>
#include <string.h>

int memo_dp[100][100];

int dp(int array, int u, int v) {
    if(memo_dp[u][v] != -1) 
        return memo_dp[u][v];

    if()


int main() {
    int trial, i, j, k, size;
    scanf("%d", &trial);
    while(trial--) {
        scanf("%d", &size);
        int edges[size][size];

        for(i = 0; i < size; i++) {
            for(j = 0; j < size; j++) {
                scanf("%d", nodes[i][j]);
            }
        }

        
    

    return 0;
}
